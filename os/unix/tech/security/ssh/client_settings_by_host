A desired set of ssh client parameters can be set per destination host with the
Host directive in /etc/ssh/ssh_config or in ~/.ssh/config, with connections to
hosts not matching the specific Host directives getting default values.
