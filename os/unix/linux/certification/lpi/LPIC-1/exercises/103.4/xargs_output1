# xargs -L 1 < xargs_input1_trailing_blank
#
# There is a trailing blank on the first line of xargs_input1.
# This trailing blank causes line concatenation to occur between the first
# and second line as xargs processes its input (the first two lines count as
# one).

1 a A 2 b B
3 c C
4 d D
