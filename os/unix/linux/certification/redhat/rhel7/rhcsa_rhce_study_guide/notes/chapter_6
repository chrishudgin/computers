291:

The number of logical drives that can be created in an extended partition is
limited only by the amount of space on the disk and the space required for each
partition.  Far more than 12 or 16 logical drives can be created, despite what
some documents claim.

311:

/sbin is a link to /usr/sbin, not /usr/bin.

329:

Most autofs configuration options, including timeout, are now in
/etc/autofs.conf, not /etc/sysconfig/autofs.
