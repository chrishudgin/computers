Show all user accounts that are members of a given group:

(RHEL) # lid -g <groupname>
(Debian) $ members <groupname>  (does not work for group "root")

The lid command is part of the libuser package.
The members command is part of the members package.
