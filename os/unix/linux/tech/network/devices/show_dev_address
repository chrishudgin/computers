To find the device addresses of network interfaces by name:

# readlink -f /sys/class/net/enp0s3

# systool -c net [-v] [enp0s3]

Is there a way to find the name of the interface given its PCI address from
lspci or lshw output without having to write a program to do it?
