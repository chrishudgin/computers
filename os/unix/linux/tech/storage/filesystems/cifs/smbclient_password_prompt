When listing SMB shares with "smbclient -L <servername>", the smbclient command
will prompt for the password of the current Linux user.  This prompt can be
ignored by pressing <Enter> without a password; the smbclient command will
still list the shares afterward.
