Generate an alphanumeric random password of a specified length in characters:

$ < /dev/urandom tr -dc 'A-Za-z0-9' | head -c <length> ; echo

Example:

$ < /dev/urandom tr -dc 'A-Za-z0-9' | head -c 8 ; echo
lS21tEmS
