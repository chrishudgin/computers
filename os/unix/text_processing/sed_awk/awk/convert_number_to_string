Convert a number variable ("myvar" in this example) to a string by
concatenating a null string to it:

myvar = 5           # value of expression is numeric
myvar ""            # value of expression is string
