String concatenation can include numeric expressions, which will be converted
into strings for the purpose of concatenation.

A concatenation of strings is a string and can therefore be used as a function
argument wherever a string is expected.

Example:

length($1 $2 $3)
