const-qualified variables are variables, not "constants."  Because they are
variables, they cannot be used as array length specifiers.  Array length
specifiers must be "constant expressions" which can contain only constants.
