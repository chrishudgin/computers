Example of remove a file to a git repository:

$ git rm foodir/barfile  (this removes the file in the filesystem as well as
   updates git information)
$ git commit -a -m "commit"
$ git push -u origin master
