To set the upper limit of CPU clock frequency, as shown by the "CPU max MHz"
field in the output of the lscpu command, set a value in MHz for the variable
arm_freq in /boot/config.txt.

For example:

# grep arm_freq /boot/config.txt
arm_freq=700

Reboot the system for the change to take effect.
